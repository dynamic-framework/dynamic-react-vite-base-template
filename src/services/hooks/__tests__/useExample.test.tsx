import { describe, it, expect } from 'vitest';

/**
 * Hooks test template
 *
 * When you add a custom hook (e.g. `useEntities`), create a sibling
 * test file `useEntities.test.tsx` here. For TanStack Query hooks,
 * wrap with QueryClientProvider; see the implementation when needed.
 */

describe('hooks placeholder', () => {
  it('placeholder — replace with real hook tests', () => {
    expect(true).toBe(true);
  });
});
